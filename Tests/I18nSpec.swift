import Foundation
import UIKit

import Nimble
import NSpry
import Quick

final class I18nSpec: QuickSpec {
    override func spec() {
        describe("I18n") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
