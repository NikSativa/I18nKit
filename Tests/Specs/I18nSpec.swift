import Foundation
import UIKit

import Quick
import Nimble
import NSpry

class I18nSpec: QuickSpec {
    override func spec() {
        describe("I18n") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
